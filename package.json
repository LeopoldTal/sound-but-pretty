{
	"name": "sound-but-pretty",
	"version": "0.1.0",
	"description": "Show audio frequency display",
	"main": "index.js",
	"license": "MIT",
	"private": true,
	"scripts": {
		"start": "esbuild ./src/ --bundle --sourcemap --outfile=www/dist/app.js --servedir=www & open http://localhost:8000/"
	},
	"dependencies": {
		"esbuild": "^0.13.12"
	}
}
