{
	"name": "noise",
	"version": "0.1.0",
	"description": "Playing around with JS audio",
	"main": "index.js",
	"license": "MIT",
	"private": true,
	"scripts": {
		"start": "esbuild ./src/ --bundle --sourcemap --outfile=www/dist/app.js --servedir=www"
	},
	"dependencies": {
		"esbuild": "^0.13.12"
	}
}
